import 'package:flutter/material.dart';

class BirdFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            'Bird Fragment',
            style: TextStyle(fontSize: 24),
          ),
      ),
    );
  }
}
